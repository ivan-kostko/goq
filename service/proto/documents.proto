syntax = "proto3";
package service;

message Empty {}

message Document {
	string identifier = 1;
	bytes content = 2;
}

message DocumentGetReuest {
	string identifier =1;
}

service Documents {
  	rpc Store (Document) returns (Empty);
	rpc Get (DocumentGetReuest) returns (Document);
}
